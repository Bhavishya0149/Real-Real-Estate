# Server
PORT=
CORS_ORIGIN=*
NODE_ENV=development

# Database
MONGODB_URI=
DB_NAME=

# JWT
ACCESS_TOKEN_SECRET=your_generated_access_token_secret_here
REFRESH_TOKEN_SECRET=your_generated_refresh_token_secret_here
ACCESS_TOKEN_EXPIRY=
REFRESH_TOKEN_EXPIRY=

# Cloudinary
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Property media limits
PHOTO_LIMIT=20
VIDEO_LIMIT=1

# Multer file size limits (in MB)
PHOTO_MAX_FILE_SIZE=5    # 5MB
VIDEO_MAX_FILE_SIZE=100   # 50MB

# Nodemailer functionality 
SMTP_USER=
SMTP_PASS=
SMTP_HOST=
SMTP_PORT=
EMAIL_FROM=